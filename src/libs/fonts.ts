import { Manrope, Kanit } from "next/font/google";

export const manrope = Manrope({ subsets: ["latin"], display: 'swap', variable: '--font-manrope' });
export const kanit = Kanit({ subsets: ["latin"], weight: ['700'], display: 'swap', variable: '--font-kanit'});