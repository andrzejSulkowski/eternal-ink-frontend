
export default defineEventHandler((event) => {
    
    // const url = getRequestURL(event);
    // console.log("URL: ", url.pathname);
    // // Check if the URL starts with /api-mock
    // if (url.pathname.startsWith('/api-mock')) {
    //   // Modify the request to use /api instead
    //   event.node.req.url = url.pathname.replace('/api-mock', '/api') + url.search;
    // }
  });
  